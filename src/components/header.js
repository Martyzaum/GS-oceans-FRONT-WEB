import styles from '../styles/header.module.scss'

const Header = () => {

    return (
        <header className={styles.headerContainer}>
            <h1>Monitoramento Oceans - Dashboards</h1>
        </header>
    );
}

export default Header;